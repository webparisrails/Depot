<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    Un mail contenant une facture sera envoyé à l'addresse: <%= @mail %> 
  </div>
  <div>
    L'addresse de livraison est la suivante:
  </div>
  <ul>
    <li><%= @name %><br></li>
    <li><%= @address %></li>
  </ul>
  <div>
    <a href="/users/edit">Modifier ces informations</a>
 <div class="field">
    <%= f.label :pay_type %><br>
    <%= f.select :pay_type, Order::PAYMENT_TYPES,
                 prompt: 'Select a payment method' %>
  </div>
  <div class="actions">
    <%= f.submit 'Place Order' %>
  </div>
<% end %>
